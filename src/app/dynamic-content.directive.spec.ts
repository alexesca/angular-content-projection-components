import { DynamicContentDirective } from './dynamic-content.directive';

describe('DynamicContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicContentDirective();
    expect(directive).toBeTruthy();
  });
});
